
        function cubeNumber(number) {
         
       if(typeof number !== "number1") {

        const cube = number * number * number;
        return cube;
        }
        else {

            return " Need input number";
        }
   
      }

        const result = cubeNumber(3);
        console.log(result);
